import React, { Component } from 'react';
// import PropTypes from 'prop-types';

class OpAdminNav extends Component {
  static defaultProps = {
  };

  static propTypes = {
  };

  state = {
  };

  render() {
    return (
      <div>OpAdminNav</div>
    );
  }
}

export default OpAdminNav;
